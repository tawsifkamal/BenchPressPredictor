# Python Project Boilerplate

A simple Python project boilerplate to get you started.

## Structure

* `src/`: Source code
* `tests/`: Tests

## Usage

1. Clone the repository
2. Add your code to the `src` directory
3. Add your tests to the `tests` directory
4. Run your tests
