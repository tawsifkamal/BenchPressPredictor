# Python Boilerplate

A simple Python boilerplate project.

## Installation

To install the necessary dependencies, run the following command:

`pip install -r requirements.txt`

## Usage

To run the main script, use the following command:

`python src/main.py`
