# tinygen

A tiny text generator.

## Installation

No installation is required. Just clone the repository and run the `main.py` script.

## Usage

To generate a tiny text, run the following command from the root of the repository:

```bash
python3 main.py
```
